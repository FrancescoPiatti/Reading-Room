{
  "name": "reading-room-workmode",
  "version": "1.0.0",
  "private": true,
  "description": "Local 'work mode' for Reading Room: serves the existing static site on loopback (127.0.0.1) and adds an integrated Claude Code terminal + live rebuild. NOT used by the static/GitHub Pages build — that stays pure-Python and dependency-free.",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "postinstall": "node ensure-pty.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "chokidar": "^3.6.0",
    "express": "^4.19.2",
    "node-pty": "^1.0.0",
    "ws": "^8.18.0"
  },
  "allowScripts": {
    "node-pty@1.1.0": true
  }
}
