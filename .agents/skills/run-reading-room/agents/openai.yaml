interface:
  display_name: "Run Reading Room"
  short_description: "Drive and screenshot the Reading Room app"
  default_prompt: "Use $run-reading-room to drive the app headlessly."

policy:
  allow_implicit_invocation: true

