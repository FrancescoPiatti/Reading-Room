interface:
  display_name: "Backup"
  short_description: "Export or restore Reading Room data"
  default_prompt: "Use $backup to export or restore Reading Room data."

policy:
  allow_implicit_invocation: false

