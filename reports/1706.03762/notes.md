This is the **example paper** that ships with the template, so the catalogue isn't empty on first launch — it also shows what a finished report (and this notes box) looks like.

- Your own notes go in files like this one (`reports/<id>/notes.md`); the build renders them and never overwrites them.
- Keep the paper, or remove it with `/remove 1706.03762` once you've added your own.
